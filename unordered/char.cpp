#include<bits/stdc++.h>
using namespace std;

int main(){
    // char name ='a';
    // char * ptr = &name;
    // cout<<(void*)ptr<<endl;

    // char arr[5]="1234";
    // char* ptr1 = arr;
    // cout<<(void*)ptr1;

    float x = static_cast<float>(10) / 3;
    cout<<x;

    return 0;
}